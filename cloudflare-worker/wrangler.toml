name = "databased-airtable-proxy"
main = "src/index.js"
compatibility_date = "2024-03-20"

[observability]
enabled = true

# The AIRTABLE_TOKEN will be stored securely as a secret in Cloudflare.
# You will set it using: npx wrangler secret put AIRTABLE_TOKEN

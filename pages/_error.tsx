/**
 * pages/_error.tsx
 *
 * Required by Next.js `output: 'export'` when the `pages/` directory exists.
 * The App Router uses `app/not-found.tsx` for 404 handling; this file
 * is only here to satisfy the static export pipeline.
 */
export default function Error() {
  return null
}
